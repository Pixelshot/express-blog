<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>
  <body>
    <!-- Syntax for importing partials. -->
    <!-- Whenever we use include() in ejs, we use - instead of = -->
    <!-- This is because equal sign(=) escapes special characters. Minus sign doesn't do that -->
    <%- include('./partials/nav.ejs') %>

    <div class="blogs content">
      <%if (blogs.length > 0) {%>
      <h2>All Blogs</h2>
      <%} else {%>
      <h2>There are no blogs to display...</h2>
      <%}%> <%if (blogs.length > 0) {%> <% blogs.map((blog) => {%>
      <h3 class="title"><%= blog.title %></h3>
      <p class="snippet"><%= blog.snippet %></p>
      <%})%> <% } %>
    </div>

    <%- include('./partials/footer.ejs') %>
  </body>
</html>
